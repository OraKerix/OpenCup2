package org.kerix.openhost.opencup.core.registry;


/**
 * UNUSED — Arena lifecycle is handled entirely by ArenaManager (core.arena).
 * <p>
 * This class was a placeholder generated during initial scaffolding.
 * It is kept to avoid breaking any imports that may reference the package,
 * but it contains no logic and should not be used.
 * <p>
 * If you need to query arena availability, use ArenaManager directly
 * (injected via Bootstrap → TournamentEngine → context).
 */
public class ArenaRegistry {
    private ArenaRegistry() {}
}
